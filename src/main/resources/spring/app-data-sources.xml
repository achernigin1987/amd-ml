<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd">

    <bean id="databaseDataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="jdbcUrl" value="${com.amd.ml.db.jdbcUrl}"/>
        <property name="driverClassName" value="${com.amd.ml.db.driver}"/>
        <property name="username" value="${com.amd.ml.db.username}"/>
        <property name="password" value="${com.amd.ml.db.password}"/>
        <property name="maximumPoolSize" value="${com.amd.ml.db.maximumPoolSize}"/>
        <property name="connectionTimeout" value="${com.amd.ml.db.connectionTimeout}"/>
        <property name="poolName" value="databasePool"/>
    </bean>
<!--
    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean id="app-datasource" parent="databaseDataSource"/>

    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="app-datasource"/>
    </bean>
    -->
</beans>